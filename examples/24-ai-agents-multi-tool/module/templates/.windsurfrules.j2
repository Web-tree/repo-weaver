# {{ project_name }} — Windsurf rules

{{ language }}.

Shared conventions live in `AGENTS.md`. Windsurf applies this file at the repository level.

## Conventions

{% for c in conventions %}
- {{ c }}
{% endfor %}
