# ~/.tmux.conf — hand-maintained.
# repo-weaver manages ONLY the region between its rw:section markers;
# everything else in this file must survive `rw apply` byte-for-byte.

set -g base-index 1            # start window numbering at 1
setw -g mouse on
set -g set-titles-string "#W/#T"

# --- my own keybindings (must be preserved) ---
bind-key -T copy-mode-vi Space send-keys -X begin-selection
