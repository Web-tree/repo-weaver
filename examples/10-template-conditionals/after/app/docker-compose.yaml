version: "3.8"

services:
  app:
    image: myapp:latest
    ports:
      - "3000:3000"
      - "8080:8080"
  database:
    image: postgres:16
    environment:
      POSTGRES_DB: appdb
      POSTGRES_USER: admin
    ports:
      - "5432:5432"
